# Legacy static site (reference only)

This folder is **not** the live app. Do not use Live Server on the project root.

## Run the real app

From the repository root:

```bash
npm install
npm run dev
```

Open **http://localhost:3000** — the Next.js app (`src/app/page.tsx`, `PreloaderLocked`, etc.).

## This folder

Archived copy of the old static `index.html` + `styles.css` + `script.js` stack. Kept for reference only.
